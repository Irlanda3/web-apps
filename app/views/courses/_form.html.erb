<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :semester %>
    <%= f.text_field :semester %>
  </div>

  <div class="field">
    <%= f.label :classroom %>
    <%= f.text_field :classroom %>
  </div>

  <div class="field">
    <%= f.label :professor_id %>
    <%= f.select(:professor_id,
    options_from_collection_for_select(Professor.all,
        :id, :name)) %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
